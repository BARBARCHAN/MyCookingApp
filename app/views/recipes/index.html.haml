.index  
  %form.search-form
    %input.search{name: "search", size: "40", type: "search", placeholder: "工事中"}
    %input.btn{name: "submit", value: "検索", type: "submit"}
  
  .pick-up
    最新の投稿
  
  %br

  .list
    - @recipes.each do |recipe|
      .upper-list
        = link_to recipe_path(recipe) do
          .image
            = image_tag recipe.image.variant(combine_options:{resize:"185x185^",crop:"185x185+0+0",gravity: :center})
            .recipe-name
              = recipe.name