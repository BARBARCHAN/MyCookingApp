.index  
  %form.search-form
    %input.search{name: "search", size: "40", type: "search", placeholder: "検索したい言葉を入力してください"}
    %input.btn{name: "submit", value: "検索", type: "submit"}

  .list
    - @recipes.each do |recipe|
      .image
        = image_tag recipe.image.variant(resize: "200x200") 
        .recipe-name
          = recipe.name